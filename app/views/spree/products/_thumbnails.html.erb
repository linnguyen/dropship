<%# no need for thumbnails unless there is more than one image %>
<% if (@product.images + @product.variant_images).uniq.size > 1 %>
  <ul id="product-thumbnails" class="thumbnails list-inline" data-hook>
    <% @product.images.each do |i| %>
      <li class='tmb-all tmb-<%= i.id %>'>
        <% img_tag = image_tag(main_app.url_for(i.url(:mini)), class: "thumbnail", alt: "Dolax Image") %>
        <%= link_to(img_tag, main_app.url_for(i.url(:product))) %>
      </li>
    <% end %>

    <% if @product.has_variants? %>
      <% @product.images_of_variants.each do |i| %>
        <% next if @product.images.include?(i) %>
        <li class='vtmb tmb-<%= i.id %>'>
          <% img_tag = image_tag(main_app.url_for(i.url(:mini)), class: "thumbnail", alt: "Dolax Image") %>
          <%= link_to(img_tag, main_app.url_for(i.url(:product))) %>
        </li>
      <% end %>
    <% end %>
  </ul>
<% end %>
